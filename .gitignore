# ── 의존성 ────────────────────────────────────────────────────────
node_modules/

# ── 실행 산출물 (감사 목적이 아닌 일별 생성 파일) ─────────────────
reports/**
!reports/*/
!reports/**/crawl_result.json
!reports/**/run_meta.json
# logs/{날짜}/ 일별 상세 로그는 제외, 누적 매니페스트(run_manifest.jsonl)는 추적 — 감사 통합 추적용
logs/*/
pipeline_run.log

# ── 환경 / 시크릿 ──────────────────────────────────────────────────
.env
.env.local
*.key

# ── Windows 임시 파일 ──────────────────────────────────────────────
~$*
*.tmp
*.tmp.*
Thumbs.db
desktop.ini

# ── macOS ──────────────────────────────────────────────────────────
.DS_Store

# ── 에디터 ─────────────────────────────────────────────────────────
.vscode/settings.json
.idea/

# ── Claude Code 내부 ───────────────────────────────────────────────
.claude/

# ── 사용 중단 (카카오 → Telegram 전환으로 폐기) ─────────────────────
notify_kakao.js
