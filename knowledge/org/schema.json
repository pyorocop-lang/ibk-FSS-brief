{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ibk.example/schemas/organization-version.json",
  "title": "IBK organization version",
  "type": "object",
  "required": ["schema_version", "version", "effective_from", "status", "sources", "structure"],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 1 },
    "version": { "type": "string", "pattern": "^[0-9]{4}-H[12]$" },
    "effective_from": { "type": "string", "format": "date" },
    "effective_to": { "type": ["string", "null"], "format": "date" },
    "status": { "enum": ["draft", "scheduled", "active", "retired"] },
    "sources": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "required": ["name", "type", "sha256"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "type": { "type": "string", "minLength": 1 },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
        },
        "additionalProperties": false
      }
    },
    "structure": {
      "type": "array",
      "items": { "$ref": "#/$defs/unit" }
    }
  },
  "$defs": {
    "unit": {
      "type": "object",
      "required": ["id", "name", "kind"],
      "properties": {
        "id": { "type": "string", "pattern": "^[A-Z0-9-]+$" },
        "name": { "type": "string", "minLength": 1 },
        "kind": { "type": "string", "minLength": 1 },
        "assignable": { "type": "boolean" },
        "children": {
          "type": "array",
          "items": { "$ref": "#/$defs/unit" }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
